import React from 'react';
import Button from '../Button/Button';

export default function DeleteButton(props) {
  return (
    <div>
      <Button className='delete-lecture'>delete</Button>
    </div>
  )
}